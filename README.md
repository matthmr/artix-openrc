# NOTE
This is the mh fork of artix's openrc package. My changes are mostly for my
use-case only.

The master branch is not where most of the action occurs, instead, look for the
relevant version you want as a tag, then see if an equivalent mh-* version
exists as well. For example: version 0.56-1 has mh-0.56-1 as its mh equivalent.
Thoses forks are kept in the pkg branch.

# Changes

## [mh-init.patch](./mh-init.patch)

1. changes to the default config file and some init files

<!-- ## [openrc-extra.patch](./openrc-extra.patch)

1. reduce the number of agetty processes from 6 to 3 -->
